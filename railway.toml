[build]
builder = "nixpacks"

[deploy]
startCommand = "pip install -r requirements.txt && playwright install chromium && uvicorn app.main:app --host 0.0.0.0 --port $PORT"

[[services]]
name = "llm-quiz-solver"
type = "web"
[http]
internalPort = 8000
